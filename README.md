# ECDSA-Nonce-Reuse-Detector-Private-Key-Recovery-r-r-Attack-
This script performs the **classic ECDSA key recovery attack** when two signatures share the **same nonce** (`k`) — revealed by identical `r` values.
